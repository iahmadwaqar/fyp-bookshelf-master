<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shinwari.registertion">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Physicologest.PhysicoMain"></activity>
        <activity android:name=".Registeration.Main2Activity" />
        <activity android:name=".Admin.AdminMain" />
        <activity android:name=".Registeration.SignUpMain" />
        <activity
            android:name=".SpleshScreen"
            android:theme="@style/Theme.Design.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UserProfile.UserProfile"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".Registeration.LoginActivity" />
        <activity android:name=".Registeration.RegisterDonater" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat" />
    </application>

</manifest>